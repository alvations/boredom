# LaTeX build artifacts (main.pdf is committed deliberately)
*.aux
*.fdb_latexmk
*.fls
*.out
*.bbl
*.blg
three-hacks/paper/*.log

# Experiment run logs ARE committed -- they are the evidence for the
# results and methodology sections. Only build logs are ignored.

# Python
__pycache__/
*.pyc

# Experiment outputs: checkpoints and derived corpora are reproducible
*.pt
.corpus_*.txt
